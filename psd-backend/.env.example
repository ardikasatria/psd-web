# Backend PSD — variabel lingkungan Fase 0
# Cocokkan dengan PSD_Checklist_Kesiapan_Rilis.md §2

# --- Inti / Auth ---
DATABASE_URL=postgresql+asyncpg://psd:psd@localhost:5432/psd
REDIS_URL=redis://localhost:6379/0
BACKEND_CORS_ORIGINS=["http://localhost:3000"]
JWT_SECRET=ganti-di-produksi
JWT_EXPIRE_MINUTES=1440

APP_BASE_URL=http://localhost:3000
COOKIE_NAME=psd_token
COOKIE_SECURE=false
COOKIE_SAMESITE=lax
# COOKIE_DOMAIN=.projeksainsdata.com
DEV_EMAIL_ECHO=true

PSD_OFFICIAL_USERNAME=psd
TOS_VERSION=2026-07

# --- Storage MinIO/S3 (Langkah 13,15,38,40,41,45,46) ---
STORAGE_ENABLED=false
MINIO_ENDPOINT=localhost:9000
MINIO_KEY=psd
MINIO_SECRET=psd-secret
MINIO_BUCKET=psd-assets
MINIO_SECURE=false

S3_ENDPOINT_URL=http://localhost:9000
S3_REGION=us-east-1
S3_ACCESS_KEY=psd
S3_SECRET_KEY=psd-secret
S3_MEDIA_BUCKET=psd-media
S3_PUBLIC_BASE_URL=http://localhost:9000/psd-media
S3_ASSETS_BUCKET=psd-assets
S3_ASSETS_PUBLIC_BASE_URL=http://localhost:9000/psd-assets
S3_SUBMISSIONS_BUCKET=psd-submissions

# --- Pencarian (Langkah 16) ---
MEILI_URL=http://localhost:7700
MEILI_KEY=psd-search-key

# --- AI (Langkah 38, 40) ---
OPENAI_API_KEY=
AI_MODEL=gpt-4o-mini

# --- Pabrik Data (Langkah 45) ---
FACTORY_RUN_TIMEOUT_S=90

# --- Celery (Langkah 49; prod aktif via deploy compose) ---
# CELERY_BROKER_URL=redis://localhost:6379/1
# CELERY_RESULT_BACKEND=redis://localhost:6379/2
# PSD_USE_CELERY=true

# --- Gitea (Langkah 50) ---
# PSD_GITEA_ENABLED=true
# PSD_GITEA_BASE_URL=http://gitea:3000
# PSD_GITEA_ADMIN_TOKEN=
PSD_OIDC_ISSUER=http://localhost:8000
# PSD_OIDC_PRIVATE_KEY="$(cat psd_oidc.pem)"
# PSD_OIDC_PRIVATE_KEY_FILE=/run/secrets/psd_oidc.pem
PSD_OIDC_KEY_ID=psd-key-1
# PSD_OAUTH_GIT_BASE_URL=https://git.projeksainsdata.com
# PSD_OAUTH_HUB_BASE_URL=https://hub.projeksainsdata.com
# PSD_HUB_ENABLED=true
# PSD_HUB_SERVICE_TOKEN=
# PSD_HUB_API_URL=http://jupyterhub:8000/hub/api
# PSD_OAUTH_BI_BASE_URL=https://bi.projeksainsdata.com

# --- MLflow (Langkah 55) ---
# PSD_MLFLOW_ENABLED=true
# PSD_MLFLOW_TRACKING_URI=http://localhost:5000
# PSD_MLFLOW_PUBLIC_URL=http://localhost:5000

# --- Serving model (Langkah 56) ---
# PSD_SERVING_ENABLED=true
# PSD_SERVING_REDIS_QUOTA=false

# --- AI Asisten (Langkah 57) ---
# PSD_ASSISTANT_ENABLED=true
# PSD_ASSISTANT_REDIS_QUOTA=false

# --- Cache & performa (Langkah 58) ---
# PSD_PERF_ENABLED=true
# PSD_PERF_CACHE_ENABLED=false
# PSD_PERF_CACHE_AUTO=true

# --- Email SMTP / Resend (Langkah 59–60) ---
# Auth: RESEND_API_KEY + PSD_EMAIL_SENDER (tanpa PSD_EMAIL_ENABLED)
# Notifikasi: PSD_EMAIL_ENABLED=true
# PSD_EMAIL_PROVIDER=http
# PSD_EMAIL_SENDER=no-reply@localhost
# RESEND_API_KEY=re_xxx
# PSD_EMAIL_SMTP_PORT=587
# PSD_EMAIL_SMTP_SSL=false
# PSD_EMAIL_UNSUBSCRIBE_SECRET=ganti-di-produksi
# PSD_EMAIL_REDIS=false
