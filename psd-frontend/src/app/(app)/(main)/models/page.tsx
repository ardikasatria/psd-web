import { ModelsPage } from '@/components/features/models/ModelsPage'
import { Metadata } from 'next'

export const metadata: Metadata = { title: 'Model' }

export default function Page() {
  return <ModelsPage />
}
