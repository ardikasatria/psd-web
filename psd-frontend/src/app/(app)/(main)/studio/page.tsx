import { StudioPage } from '@/components/features/studio/StudioPage'
import { Metadata } from 'next'

export const metadata: Metadata = { title: 'Studio Instruktur' }

export default function Page() {
  return <StudioPage />
}
